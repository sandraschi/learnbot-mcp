[package]
name = "learnbot-mascot"
version = "0.1.0"
description = "Transparent VRM desktop mascot — Miko-chan on your screen"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
